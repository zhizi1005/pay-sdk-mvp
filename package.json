{
  "name": "pay-sdk-mvp",
  "version": "1.1.1",
  "description": "Embeddable browser JS RampPay SDK for Google Pay and Apple Pay",
  "type": "module",
  "types": "dist/types/index.d.ts",
  "scripts": {
    "dev": "vite --config vite.demo.config.ts",
    "demo": "npm run build && vite --config vite.demo.config.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build && tsc --emitDeclarationOnly",
    "preview": "npm run build && vite --config vite.demo.config.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,js,json,md,html,css}": "prettier --write"
  },
  "devDependencies": {
    "@types/applepayjs": "^14.0.0",
    "@types/googlepay": "^0.7.6",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8",
    "prettier": "^3.9.5",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@fingerprintjs/fingerprintjs-pro": "^3.12.13"
  }
}
